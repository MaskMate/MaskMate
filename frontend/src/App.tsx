import { FC } from "react";
import NavigationBar from "@components/Navbar";

interface AppProps {}

const App: FC<AppProps> = () => {
    return (
        <>
            <NavigationBar />
        </>
    );
};

export default App;
